# MSAgent 体验报告

## 一、报告基本信息

| 项目 | 内容 |
|------|------|
| 报告编号 | MSAgent-20260819-001 |
| 体验日期 | 2026-08-19 |
| Agent版本 | MSAgent（具体版本未提供） |
| Profiling数据来源 | Ascend NPU 4 卡集群 Profiling 数据，包含 `cluster_analysis_output`、`StepTraceTime`、`SlowRank`、`dispatch_view`、`free_analysis` 等分析结果 |
| 测试目标 | 通过多轮自然语言交互，验证 Agent 对 Ascend 集群 Profiling 数据进行快慢卡识别、根因定位、影响量化及优化建议的分析能力 |

---

## 二、交互记录

### 第 1 轮交互

| 项目 | 内容 |
|------|------|
| 输入 Prompt | 从当前Profiling数据来看，有无集群快慢卡，有什么关键证据 |
| Agent 输出（文字摘要） | Agent 判断存在明显集群快慢卡，主要为 rank1 和 rank2，其中 rank1 为主要慢卡、rank2 为次要慢卡。通过 `SlowRank`、`ClusterStepTraceTime`、HCCL 通信、同步 API、Host 下发以及算子统计等多维数据建立证据链，并判断问题属于 Host 下发瓶颈型慢卡，而非设备计算或慢链路问题。 |
| **输出截图** | ![第1轮交互截图](./交互截图_1.png) |
| 是否符合预期 | 是 |
| 评价 | 能够快速识别快慢卡并给出较完整的证据链。重点引用了 `slowAffectCount`、Free 占比、allReduce、`aclrtSynchronizeEvent`、Host 侧 API 以及 `slow_link` 等指标，分析较全面。 |

### 第 2 轮交互

| 项目 | 内容 |
|------|------|
| 输入 Prompt | Profiler > 造成快慢卡的原因是什么 |
| Agent 输出（文字摘要） | Agent 进一步下钻 Host 下发慢的具体根因。通过 `dispatch_view`、CANN API、`launch` 长尾、Free 时间以及 trace 时间对齐进行分析，发现 rank1 的 Host 下发阻塞更频繁，并且 45.4ms 的 launch 阻塞与约 46.6ms 的 NPU 空闲段高度对齐，由此建立“Host launch 阻塞 → NPU 饿死 → 集合通信等待 → 拖慢其他 Rank”的因果链。进一步提出 CPU 绑核、关闭在线编译、启用二级下发流水线、降低下发频率等优化建议。 |
| **输出截图** | ![第2轮交互截图](./交互截图_2.png) |
| 是否符合预期 | 是 |
| 评价 | 能够从宏观快慢卡判断进一步深入到 Host 调度和算子下发层面，且通过跨数据源的时间对齐增强了结论可信度。尤其是将 rank1 的 45.4ms launch 与 46.6ms Free 段进行对应，定位较有说服力。 |

### 第 3 轮交互

| 项目 | 内容 |
|------|------|
| 输入 Prompt | Profiler > 评估快慢卡问题造成的影响，拖慢了多少时间 |
| Agent 输出（文字摘要） | Agent 对快慢卡造成的性能影响进行了量化。根据 `StepTraceTime` 得出实际单步时间约 1414ms，理想情况下约 537ms，因此每个训练步约被拖慢 877ms，实际步时间约为理想下限的 2.6 倍。进一步估算每 1000 步约增加 14.6 分钟，并给出实际约 0.71 step/s、理想约 1.86 step/s 的吞吐对比，以及修复后理论上最高约 2.6 倍的加速空间。 |
| **输出截图** | ![第3轮交互截图](./交互截图_3.png) |
| 是否符合预期 | 是 |
| 评价 | 能够将定性问题进一步转化为量化性能损失，分析结果具有较强的工程参考价值。同时说明了估算边界，指出实际收益取决于 Host 下发瓶颈是否能够完全消除。 |

---

## 三、多轮交互整体评价

| 评价维度 | 评分（1~5） | 说明 |
|----------|-------------|------|
| 问题理解准确性 | 5 | 能够准确理解“快慢卡识别→根因定位→影响量化”的递进式分析需求，并持续围绕 Profiling 数据展开。 |
| 数据分析深度 | 5 | 能够综合使用 SlowRank、StepTraceTime、HCCL、CANN API、dispatch、Free 分析等多维数据，而不是只依据单一指标判断。 |
| 证据链条完整性 | 5 | 从慢卡识别、Host launch 阻塞、NPU Free，到集合通信等待和整体性能损失，形成了较完整的因果链。 |
| 优化建议实用性 | 5 | 给出了 CPU 绑核、关闭在线编译、TASK_QUEUE_ENABLE、增大 Batch/梯度累积等具有工程可操作性的建议，并区分了优先级。 |
| 多轮对话连贯性 | 5 | 后续问题能够继承上一轮结论继续下钻，没有重复从头分析，能够保持上下文连续性。 |
| 响应速度与稳定性 | 4 | 多次 Profiling SQL 和分析工具调用均能完成，但单次分析涉及较多工具调用，整体响应耗时仍有进一步优化空间。 |
| 整体满意度 | 5 | 基本达到预期，能够从 Profiling 原始数据逐步完成识别、定位和量化，对性能优化具有较高参考价值。 |

---

## 四、问题与改进建议

### 发现的问题

1. **部分根因仍缺少直接证据。**  
   Agent 判断 CPU 调度干扰/资源竞争是较可能的根因，但当前 `_host_sys` 为空，没有 sched slice 或线程切换记录，因此无法 100% 确认是 CPU 抢占还是其他系统干扰。

2. **动态 Shape 根因定位还不够具体。**  
   Advisor 只检测到 1 个动态 Shape 算子，但没有进一步给出具体算子名称和代码位置，因此还需要结合更完整的 Profiling 数据继续定位。

3. **部分性能提升数据属于理论估算。**  
   例如“每步拖慢约 877ms”和“理论最高约 2.6 倍加速”是在假设 Host 下发饥饿能够完全消除的情况下得到的，实际优化收益仍需要修复后重新 Profiling 验证。

### 改进建议

1. **增强 Host 侧 Profiling 数据采集。**  
   建议重新采集时开启 Host 系统侧数据或 `with_stack=True`，进一步获取线程调度、CPU 抢占以及调用栈信息，从而将“Host 调度瓶颈”继续定位到具体线程、函数或代码位置。

2. **加强优化前后的自动对比能力。**  
   在修复后自动对比 rank1/rank2 Free 占比、SlowRank 影响次数、实际 Step 时间、allReduce 耗时以及 `aclrtSynchronizeEvent` 等指标，形成优化前后量化报告。

3. **进一步提升分析结果的可视化和可读性。**  
   可将关键指标自动生成 Rank 对比表、时间线和因果关系图，帮助用户更直观地理解“Host 下发阻塞 → NPU 空闲 → 通信等待 → 集群性能下降”的完整链路。
