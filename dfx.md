# msagent DFX 优化记录

| 场景 | 原报错及原因 | DFX 修改方案 |
| --- | --- | --- |
| Agent 处理包含 `error` 的日志时，流式模型响应失败 | 原报错：`Error processing message: No generations found in stream`。该错误表示 OpenAI 兼容模型服务返回了空的流式响应，LangChain 无法聚合出 generation；并非日志文本中出现 `error` 本身导致。原始异常对用户缺少可行动说明。 | 在消息错误格式化中识别该异常，提示“模型流式接口返回空响应、本次请求未完成、会话仍可继续、请稍后重试”。保留完整异常链、线程、Agent、模型等信息写入日志，便于定位服务端空流问题。 |
| Agent 执行任务时模型服务触发 429 限流 | 原报错：`Error processing message: Error code: 429`。现有 `retry.model.max_retries` 已由底层 LLM 客户端执行；最终出现 429 表示配置的重试次数仍未恢复。原始报错未说明重试状态和会话影响。 | 不新增第二套重试逻辑，继续使用既有 `retry.model.max_retries`。在消息错误格式化中识别 429、`Error code: 429` 和 rate-limit 信息，提示“配置的重试已耗尽、本次请求未完成、会话仍可继续、请稍后重试”；详细 HTTP/模型上下文保留在日志中。 |
| 上下文过长触发自动压缩时，tokenizer 下载因 SSL 证书失败 | 原报错：`Error compressing conversation: HTTPSConnectionPool(...openaipublic.blob.core.windows.net...cl100k_base.tiktoken...CERTIFICATE_VERIFY_FAILED...)`。LLM tokenizer 不支持计数后会尝试加载 `tiktoken` 的 `cl100k_base`；首次加载需要联网下载，内网自签名证书使下载失败。原逻辑未覆盖 LLM tokenizer 的这类运行时异常，且直接将底层 URL/证书异常展示给用户。 | token 统计改为：LLM tokenizer 失败时尝试 `tiktoken`，仍失败时使用离线字符估算，避免辅助计数阻断压缩。压缩失败时终端仅提示“压缩未完成，已返回结果和现有会话已保留；达到上下文限制时可新建会话”，详细异常写入 warning 日志，不再直接暴露 SSL 细节。 |
| Agent 请求模型服务时 DNS 解析失败 | 原报错包括：Linux 环境的 `Error processing message: Connection error. Cause: ConnectError: [Errno -3] Temporary failure in name resolution`、`Error processing message: Connection error. Cause: ConnectError: [Errno -2] Name or service not known`，以及 Windows 环境常见的 `ConnectError: [Error 11001] getaddrinfo failed` 或 `ConnectError: [Error 11001] get addrinfo failed`。它们都表示运行环境无法将模型服务或代理域名解析为 IP；Linux `-3` 通常是 DNS 暂时不可用，`-2` 更偏向域名不存在、拼写错误或 DNS 配置中没有该记录，Windows `11001` 也表示主机名解析失败。若处理 CA/证书后恢复，通常说明网络、代理和证书信任链存在组合问题；证书校验不是这些 DNS 错误码的直接原因。原始报错只暴露底层连接异常，用户难以判断排查方向。 | 在消息错误格式化中识别 `[Errno -3]`、`Temporary failure in name resolution`、`[Errno -2]`、`Name or service not known`、Windows `11001`、`getaddrinfo failed` 及 `get addrinfo failed` 等 DNS 异常，提示“模型服务或代理域名无法解析，请检查网络、DNS、模型 `base_url` 和代理配置后重试”。不增加第二套重试，不新增代理或网络日志采集。 |
| Agent 通过代理访问模型服务时，代理返回 504 网关超时 | 原报错：`Error processing message: Connection error. Cause: ProxyError: 504 Gateway Time-out`。请求已到达 HTTP/HTTPS 代理，但代理在超时时间内未能从上游模型服务获取响应；常见于代理到模型服务的网络链路、VPN/企业网络、代理路由或模型服务暂时不可用。原始报错未明确故障发生在代理网关。 | 在消息错误格式化中识别 `ProxyError: 504`、`504 Gateway Time-out` 和 `504 Gateway Timeout`，提示“代理网关连接模型服务超时，请检查 HTTP/HTTPS 代理、VPN 或企业网络连接，以及模型服务可用性后重试”。不调整现有重试、代理配置或请求超时。 |
| Agent 连接模型服务或代理时，TLS 证书校验失败 | 原报错：`Error processing message: Connection error; [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: unable to get local issuer certificate (_ssl.c:1006)`。模型服务或企业代理提供的证书链不受当前运行环境信任；常见于企业代理替换 HTTPS 证书、服务端使用自签名证书或证书链不完整、运行环境缺少企业根 CA。原始报错只暴露底层 OpenSSL 异常。 | 在消息错误格式化中识别 `CERTIFICATE_VERIFY_FAILED`、`certificate verify failed` 和 `unable to get local issuer certificate`，提示“连接模型服务或代理时 TLS 证书校验失败，当前环境不信任该证书链；请检查服务端或代理 CA 证书及 SSL 校验配置后重试”。不关闭证书校验，不修改 `verify` 配置或网络行为。 |
| Agent 读取模型服务或远程工具响应时连接中断 | 原报错：`Error processing message: ReadError`。该异常通常来自 `httpx/httpcore` 的 HTTP 响应读取阶段，表示连接已建立但在接收响应或流式数据时中断；常见于网络抖动、代理/VPN/企业网络重置连接、模型服务或远程工具服务提前关闭连接。它通常不是本地文件读取失败；本地文件问题一般会显示 `FileNotFoundError`、`PermissionError` 或路径相关异常。 | 在消息错误格式化中识别异常类型 `ReadError`，提示“读取模型服务或远程工具响应时连接中断，请检查网络、代理或 VPN 稳定性及服务可用性后重试”。不调整现有重试、超时、代理或工具执行逻辑。 |
| Agent 无法建立到模型服务或代理的连接 | 原报错：`Error processing message: Connection error. Cause: ConnectError: All connection attempts failed`。域名通常已完成解析，但客户端尝试建立 TCP/HTTPS 连接均失败；常见于模型 `base_url` 或端口错误、模型服务未启动、网络路由或防火墙拦截、代理或 VPN 不可用。该错误发生在连接建立阶段，不同于 DNS、TLS 校验或响应读取中断。 | 在消息错误格式化中识别异常类型 `ConnectError` 和 `All connection attempts failed`，提示“无法连接模型服务或代理，请检查网络、模型 `base_url`、代理或 VPN 设置、防火墙访问和服务可用性后重试”。不调整现有重试、超时、代理或工具执行逻辑。 |
| 自动压缩下载 `cl100k_base` tokenizer 编码失败 | 原报错包括：`Error compressing conversation: HTTPConnectionPool(...openaipublic.blob.core.windows.net.../encodings/cl100k_base.tiktoken...ConnectTimeoutError...)`，以及 `...Caused by SSLError(SSLCertVerificationError...)`。本地没有缓存 `cl100k_base` 编码文件时，`tiktoken` 或其调用方会尝试联网下载；前者表示网络、代理或 VPN 访问超时，后者表示 tokenizer 服务或企业代理的 CA 证书链不受当前环境信任。该错误可能来自压缩流程内部依赖的 tokenizer 调用。 | 在压缩异常处理里识别 `cl100k_base.tiktoken`：连接超时时提示“tokenizer 编码下载超时，请检查访问 tokenizer 服务的网络或代理”；TLS 证书校验失败时提示“请检查 tokenizer 服务或代理 CA 证书及 SSL 校验配置”。两种提示都会说明已返回结果和现有会话已保留；不再细分具体证书链子类型，也不修改 tokenizer 下载、网络、代理、重试或压缩逻辑。 |
| Agent 处理过程中 JSON 解析失败（暂不适配） | 原报错：`Error processing message: Expecting value: line 1 column 1 (char 0)`。这是 `json.JSONDecodeError`，表示代码期望 JSON，但收到空内容、HTML 错误页、纯文本错误、截断数据或其他无效 JSON。该异常可能来自模型服务/代理响应、MCP/远程工具响应、工具参数或配置解析；外层错误文本没有保留具体解析来源。 | 暂不增加通用 DFX 适配。仅凭该文本无法可靠判断责任方，若直接提示模型、代理或工具之一会误导用户。需要先通过 `app.log` 的 `exception_chain`、请求/响应上下文和堆栈定位实际解析调用点；后续如确认来源，再在对应调用点补充“模型响应 JSON”“远程工具 JSON”或“工具参数 JSON”的精确提示。 |
