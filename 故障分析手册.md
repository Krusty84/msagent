# msagent 故障分析手册

本文按故障发生阶段整理。排查时应优先匹配具体错误关键字，不要仅根据外层的 `Error processing message`、`Connection error` 或 `Error starting chat session` 判断根因。

## 1. 模型连接报错

### 1.1 `ConnectError`

#### 错误特征与可能原因

| 错误特征 | 可能原因 |
| --- | --- |
| `[Errno -3] Temporary failure in name resolution` | DNS 服务暂时不可用，模型服务或代理域名当前无法解析。 |
| `[Errno -2] Name or service not known` | 域名不存在、拼写错误或当前 DNS 中没有对应记录。 |
| `[Error 11001] getaddrinfo failed` | Windows 环境下的主机名解析失败。 |
| `All connection attempts failed` | 域名可能已经成功解析，但客户端无法与模型服务或代理建立 TCP/HTTPS 连接。常见于 `base_url` 或端口错误、服务未启动、防火墙拦截、路由不可达，以及代理或 VPN 不可用。 |

#### 处理方法

1. 检查当前环境能否正常访问网络。
2. 核对模型 `base_url` 的协议、域名和端口，以及代理地址是否正确。
3. 检查 DNS、网络路由、防火墙、企业网络、VPN 和代理配置。
4. 确认模型服务或代理服务已经启动并正常监听。
5. 若问题偶发，可在网络或 DNS 恢复后重试。
6. 日志默认写入 `<工作目录>/.msagent/logs/app.log`。使用 `msagent -v` 可在启动时显示实际日志路径。可检索 `exception_chain=`、`request=` 和 `base_url=`；需要 DEBUG 日志时，在启动前设置 `MSAGENT_LOG_LEVEL=DEBUG`。

#### 检查模型配置

msagent 使用工作目录下的项目配置。切换工作目录后，应重新检查当前目录实际生效的配置：

```bash
msagent config --show
```

重点核对输出中的 `LLM Provider`、`Model` 和 `Base URL`。以下示例中的地址、模型名称和 API Key 均为占位值，执行前需要替换。

#### Linux 接口验证

```bash
export API_KEY="your-api-key"
export MODEL_NAME="your-model-name"
export MODEL_BASE_URL="https://your-model-service.example.com/v1"

curl --request POST \
  --url "${MODEL_BASE_URL}/chat/completions" \
  --header "Authorization: Bearer ${API_KEY}" \
  --header "Content-Type: application/json" \
  --data '{
    "model": "'"${MODEL_NAME}"'",
    "messages": [
      {
        "role": "user",
        "content": "你好"
      }
    ],
    "stream": false
  }'
```

如果正常请求报证书校验错误，可临时增加 `-k` 进行对比测试：

```bash
curl -k --request POST \
  --url "${MODEL_BASE_URL}/chat/completions" \
  --header "Authorization: Bearer ${API_KEY}" \
  --header "Content-Type: application/json" \
  --data '{
    "model": "'"${MODEL_NAME}"'",
    "messages": [
      {
        "role": "user",
        "content": "你好"
      }
    ],
    "stream": false
  }'
```

`-k` 会跳过 TLS 证书校验，仅用于确认问题是否与证书信任链有关，不应作为正式配置。如果普通请求失败而 `-k` 请求成功，应按 `CERTIFICATE_VERIFY_FAILED` 排查；DNS 解析失败时，使用 `-k` 也无法建立连接。

#### Windows CMD 接口验证

```bat
set "API_KEY=your-api-key"
set "MODEL_NAME=your-model-name"
set "MODEL_BASE_URL=https://your-model-service.example.com/v1"

curl.exe --request POST ^
  --url "%MODEL_BASE_URL%/chat/completions" ^
  --header "Authorization: Bearer %API_KEY%" ^
  --header "Content-Type: application/json" ^
  --data "{\"model\":\"%MODEL_NAME%\",\"messages\":[{\"role\":\"user\",\"content\":\"你好\"}],\"stream\":false}"
```

#### Windows PowerShell 接口验证

```powershell
$ApiKey = "your-api-key"
$ModelName = "your-model-name"
$ModelBaseUrl = "https://your-model-service.example.com/v1"
$Body = @{
    model = $ModelName
    messages = @(
        @{
            role = "user"
            content = "你好"
        }
    )
    stream = $false
} | ConvertTo-Json -Depth 4 -Compress

Invoke-RestMethod `
  -Method Post `
  -Url "$ModelBaseUrl/chat/completions" `
  -Headers @{ Authorization = "Bearer $ApiKey" } `
  -ContentType "application/json" `
  -Body $Body
```

### 1.2 TLS 证书校验失败

#### 错误特征

- `CERTIFICATE_VERIFY_FAILED`

#### 可能原因

当前运行环境无法验证模型服务或企业代理提供的 TLS 证书链。常见原因包括企业代理替换 HTTPS 证书、服务端使用自签名证书、服务端证书链不完整，或者运行环境缺少企业根 CA。

#### 处理方法

1. 检查模型服务提供的证书及完整证书链。
2. 若使用企业代理，确认运行环境已经安装并信任企业根 CA。
3. 检查代理 CA 和 SSL 校验配置。
4. 可使用上一节的 `curl -k` 做一次对比测试，但不应通过长期关闭证书校验规避问题。

### 1.3 `ProxyError: 504`

#### 错误特征

- `ProxyError: 504`

#### 可能原因

请求已经到达 HTTP/HTTPS 代理，但代理在规定时间内没有从上游模型服务获得响应。常见于代理到模型服务的网络异常、VPN 或企业网络不稳定、代理路由错误，或者模型服务暂时不可用。

#### 处理方法

1. 检查 HTTP/HTTPS 代理地址和代理服务状态。
2. 检查 VPN 或企业网络连接。
3. 确认模型服务可用，并检查代理到模型服务的网络链路。
4. 链路恢复后重新执行请求。

### 1.4 模型响应读取失败

#### 错误特征与可能原因

| 错误特征 | 可能原因 |
| --- | --- |
| `ReadTimeout` | 连接通常已经建立，但在读取超时时间内没有收到响应或后续流式数据。常见于模型生成过慢、服务负载过高、网络或代理延迟较大。 |
| `ReadError` | 读取响应或流式数据时连接中断。常见于网络抖动、连接被服务端或代理重置、代理或 VPN 不稳定，以及模型服务提前关闭连接。该异常通常不是读取本地文件失败。 |

#### 处理方法

1. 检查模型服务负载、响应耗时和服务端日志。
2. 检查网络、代理和 VPN 的延迟与稳定性。
3. 对于 `ReadTimeout`，确认读取超时配置是否适合模型生成耗时。
4. 对于 `ReadError`，检查服务端或代理是否提前关闭或重置连接。
5. 服务或链路恢复后重新执行请求。

## 2. 模型运行时报错

### 2.1 HTTP `429`

#### 错误特征

- `Error code: 429`

#### 可能原因

模型服务触发了调用频率、并发数或配额限制。msagent 已通过 `retry.model.max_retries` 使用底层模型客户端进行重试；最终仍显示 429，表示配置的重试次数执行完后服务仍未恢复。

#### 处理方法

1. 稍后重新执行请求。
2. 检查模型服务的调用频率、并发限制和账户配额。
3. 降低同时运行的模型请求数量。
4. 不需要在 msagent 外层再增加一套相同的重试逻辑。

### 2.2 HTTP `400`：thinking 消息格式不兼容

#### 错误特征

- `Error code: 400` + `missing field 'thinking'`

#### 可能原因

模型服务在校验会话消息时，将其中一条 `content` 识别为 thinking 消息，但消息中缺少服务端要求的 `thinking` 字段。可能是当前模型的推理消息协议与 OpenAI 兼容接口不一致，也可能是历史会话中保留了与当前模型不兼容的推理消息。

#### 处理方法

1. 新建会话后重新发送请求；若新会话正常，说明旧会话历史消息格式不兼容。
2. 若新会话仍然报错，核对模型名称、模型推理能力、接口协议和 `base_url` 是否匹配。
3. 日志默认写入 `<工作目录>/.msagent/logs/app.log`，使用 `msagent -v` 可在启动时显示实际日志路径。检查其中的 `resolved_model=`、`base_url=`、`status_code=` 和 `response_body=`；需要 DEBUG 日志时，在启动前设置 `MSAGENT_LOG_LEVEL=DEBUG`。

### 2.3 `No generations found in stream`

#### 错误特征

- `No generations found in stream`

#### 可能原因

OpenAI 兼容模型服务返回了空的流式响应，客户端无法从响应流中聚合出模型结果。该问题与用户输入或日志文本中包含单词 `error` 没有直接关系。

#### 处理方法

1. 稍后重新执行请求，当前会话仍可继续使用。
2. 检查模型服务的流式接口是否返回了有效数据。
3. 查看模型服务日志，确认是否存在空响应、流式连接提前关闭或服务端生成失败。
4. 若持续复现，确认当前模型接口是否完整兼容 OpenAI 流式响应格式。

## 3. 上下文压缩报错

### 3.1 tokenizer 编码下载失败

#### 错误特征与可能原因

| 错误特征 | 可能原因 |
| --- | --- |
| `cl100k_base.tiktoken` + `ConnectTimeoutError` | 本地没有缓存 tokenizer 编码文件，下载时因网络、代理或 VPN 不可用而超时。 |
| `cl100k_base.tiktoken` + `SSLCertVerificationError` | tokenizer 下载服务或企业代理的 CA 证书链不受当前环境信任。 |

这类错误发生在会话压缩所依赖的 tokenizer 下载链路，不等同于模型服务连接失败。压缩失败时，已返回的结果和现有会话仍会保留。

#### 处理方法

1. 确认当前环境能够访问 tokenizer 编码下载服务。
2. 检查网络、HTTP/HTTPS 代理和 VPN 配置。
3. 若包含超时关键字，检查下载链路是否被防火墙阻断或响应过慢。
4. 若包含证书关键字，检查 tokenizer 服务或企业代理的 CA 证书和 SSL 校验配置。
5. 会话接近上下文上限且压缩持续失败时，可新建会话继续操作。

## 4. Agent、工具与启动报错

### 4.1 外层错误无法直接确定根因

#### 错误特征与可能原因

| 错误特征 | 可能原因 |
| --- | --- |
| `Expecting value: line 1 column 1 (char 0)` | 程序期望解析 JSON，但收到空内容、HTML 错误页、纯文本错误、截断数据或其他无效 JSON。来源可能是模型服务、代理、MCP、远程工具、工具参数或配置。 |
| `unhandled errors in a TaskGroup` | 这是 Python/AnyIO 并发任务失败后的外层 `ExceptionGroup` 摘要。真实根因位于 sub-exception 中，可能来自 MCP 连接、模型或 Agent 图初始化、配置、Skill 加载或 SQLite checkpointer。 |

#### 处理方法

1. 不要仅根据外层错误猜测责任组件。
2. 日志默认写入 `<工作目录>/.msagent/logs/app.log`，使用 `msagent -v` 可在启动时显示实际日志路径；需要 DEBUG 日志时，在启动前设置 `MSAGENT_LOG_LEVEL=DEBUG`。
3. 对 JSON 解析错误，检查 `exception_chain=`、`request=`、`status_code=`、`response_body=` 和完整堆栈，确认实际解析来源。
4. 对 `TaskGroup` 错误，查找完整 `ExceptionGroup` 堆栈中的首个有效 sub-exception，再按对应组件继续排查。

### 4.2 SQL 工具参数校验失败

#### 错误特征

- `validation error for execute_sql_queryArguments trace_path`

#### 可能原因

`execute_sql_query` 的输入 schema 校验发现必填参数 `trace_path` 缺失、为空或格式无效。此时工具尚未执行 SQL、打开数据库或加载 SQLite，因此该错误通常不是 SQLite 未安装。`6 attempts` 表示现有工具重试重复执行了无法自行恢复的参数校验错误。

#### 处理方法

1. 检查 MCP 工具的实际输入 schema，确认 `trace_path` 是否为必填字段。
2. 确认传入的是有效的 profiling trace 路径，并且运行环境有权访问该路径。
3. 检查 Agent 生成工具参数的上下文，确认 `trace_path` 是否被遗漏或改写。
4. 在参数问题修正前，重复执行相同调用不会恢复。

### 4.3 YAML 配置语法错误

#### 错误特征

- `mapping values are not allowed here`

#### 可能原因

YAML 配置存在明确的语法错误。常见原因包括缩进不一致、使用 Tab、列表或映射结构错误，以及包含 `:` 的值未正确使用引号或多行 YAML 语法。完整异常通常会同时给出文件位置和行号。

#### 处理方法

1. 根据原始异常中的文件路径和行号定位配置项。
2. 检查相邻配置的缩进层级，统一使用空格而不是 Tab。
3. 检查列表、映射和包含冒号的字符串格式。
4. 修正 YAML 后重新启动 msagent。
